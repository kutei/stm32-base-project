name: cpp-build

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: prepare
        run: |
          apt install arm-none-eabi-gcc
      - name: make build
        run: make
